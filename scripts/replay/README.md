# replay scripts

Replay utilities should:
- validate `replay-ledger.json` against `schemas/replay-ledger.schema.json`,
- reconstruct execution state from artifacts,
- export normalized run records for analytics and audit trails.
