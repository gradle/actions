# telemetry scripts

Telemetry utilities should capture:
- startup duration,
- dependency resolution time,
- failure points and retries,
- total task duration,
then package metrics for Orion ingestion.
