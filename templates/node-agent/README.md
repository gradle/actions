# node-agent template

Baseline Node.js agent template for DREDGE execution workloads.

## Contract
- Produces structured artifacts for replay + telemetry.
- Supports role-aware execution (`forge`, `gate`, `sentinel`, `replay`, `oracle`).
- Supports Orion payload emission for centralized control-plane ingestion.
